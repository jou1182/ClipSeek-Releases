# ClipSeek — دليل الاستخدام الشامل

**الإصدار:** 1.0.0 · **نظام التشغيل:** Windows

> ClipSeek تطبيق يبحث عن أي كلمة أو عبارة داخل ملفات الترجمة (SRT/VTT/TXT) عبر حلقاتك التعليمية كلها دفعة واحدة، ويقفز بك إلى **اللحظة الدقيقة** في الفيديو التي وردت فيها الكلمة.

---

## 1) التشغيل

| الطريقة | الخطوات |
|---|---|
| **السكربت (موصى به)** | انقر نقرًا مزدوجًا على `Start_ClipSeek.vbs` — يفتح التطبيق بدون نافذة أوامر |
| **من الطرفية** | `python main.py` (من مجلد المشروع) |
| **النسخة الجاهزة** | شغّل `dist/ClipSeek.exe` (ملف واحد، لا يحتاج Python) |

> **ملاحظة:** إن استخدمت نسخة قديمة من `dist/ClipSeek.exe` فلن ترى أحدث الميزات — استخدم `Start_ClipSeek.vbs` الذي يشغّل الكود المحدَّث مباشرة.

---

## 2) سير العمل الأساسي (3 خطوات)

### الخطوة 1 — جهّز ملفات الترجمة (باستخدام Buzz)
1. افتح **Buzz** وولّد ترجمة لحلقاتك (اختر `Faster Whisper` + `Large-V3-Turbo` + `Auto detect` + `Transcribe`)
2. صدّر النتائج بصيغة **SRT** (وTXT إن أردت)
3. ضع ملفات SRT **في نفس مجلد الفيديوات** (أو مجلد فرعي مثل `videos/`)
4. لا يهم إن أضاف Buzz لاحقة `(transcribed on ...)` للاسم — ClipSeek يتجاهلها تلقائيًا

### الخطوة 2 — اختر المجلد
- اضغط **"تصفح"** واختر مجلد الحلقات، أو **اسحب المجلد** إلى التطبيق
- ستظهر إحصائيات الملفات (عدد SRT/VTT/TXT)

### الخطوة 3 — ابحث
- اكتب الكلمة في خانة البحث — **النتائج تظهر تلقائيًا أثناء الكتابة** (بعد توقف قصير)
- أو اضغط **"بحث"** للبحث الفوري
- اضغط **"▶ فتح"** على أي نتيجة — يفتح الفيديو عند **اللحظة المحددة** عبر VLC (أو MPV)

---

## 3) أوضاع البحث الخمسة

| الوضع | الوظيفة | مثال |
|---|---|---|
| **يحتوي على** (افتراضي) | بحث نصي فرعي، مع تجاهل التشكيل تلقائيًا للعربية | `متغير` يطابق `مُتَغيّر` |
| **مطابقة تامة** | الكلمة كاملة فقط (لا تطابق كلمة ضمن كلمة أطول) | `متغير` لا يطابق `المتغير` |
| **Regex** | تعبيرات نمطية | `chapter\d` يطابق `chapter1` |
| **Aa** | تجاهل حالة الأحرف | `hello` يطابق `HELLO` |
| **عربي (بدون تشكيل)** | تطبيع عربي كامل دائمًا | `يحيى` يطابق `يحيي` |

### 🔍 بحث بعبارات
اكتب العبارة بين علامتي اقتباس لتبحث عنها **كاملة كوحدة واحدة**:
```
"قواعد البيانات"
```
- يطابق فقط الحلقات التي وردت فيها العبارة كاملة
- يعمل مع التشكيل (يتجاهله) ومع الإنجليزية
- لا تظهر نتائج مشتتة عن "قواعد" أو "البيانات" منفصلتين

---

## 4) الميزات المتقدمة

### 📚 المكتبة (بحث في عدة مجلدات دفعة واحدة)
1. اضغط زر **"📚 المكتبة"** (أعلى النتائج)
2. اضغط **"إضافة مجلد"** وأضف كل سلسلة حلقات لديك
3. الآن أي بحث يغطي **كل المجلدات معًا** — تكتب الكلمة مرة فتجدها في كل السلسلات
4. أزرار **"إزالة"** و**"مسح الكل"** لإدارة القائمة

### 📊 الملخص (أين وردت الكلمة؟)
1. ابحث عن أي كلمة
2. اضغط **"📊 الملخص"** (يتفعّل عند وجود نتائج)
3. تظهر قائمة بالحلقات **مرتبة من الأكثر تكرارًا** للكلمة + إجمالي المرات
4. مثالي لمعرفة أي حلقة تحتاج مشاهدتها أولًا

### 🎯 ترتيب النتائج بالأهمية
- النتائج تظهر افتراضيًا **بالأهمية**: الأكثر تكرارًا للكلمة أولًا، ثم القريبة من بداية الجملة، ثم SRT/VTT قبل TXT
- يمكنك الفرز يدويًا بالضغط على أي عمود (يُحفظ الفرز)

### 🔎 تصفية النتائج
- **فلاتر النوع:** `كل النتائج / SRT / VTT / TXT` (مع العدّاد)
- **بحث داخل النتائج:** `Ctrl+L` أو `/` — يصفّي النتائج الظاهرة بكتابة كلمة (اسم الملف أو النص)

### 👁 المعاينة
- عند تحديد نتيجة تظهر في الأسفل: السياق **قبل المطابقة → المطابقة → بعدها**
- زر **"فتح"** يشغّل الفيديو عند التوقيت

### 📤 التصدير
- **تصدير Excel** (`Ctrl+E`) / **تصدير HTML** / **نسخ الكل** — يصدّر النتائج الظاهرة (مع احترام الفلاتر)
- يدعم العربية والإنجليزية (رؤوس مترجمة + اتجاه RTL/LTR)

---

## 5) الإعدادات

افتحها بزر **"S"** أعلى اليسار:

| المجموعة | الخيارات |
|---|---|
| **المظهر واللغة** | اللغة (عربي/English)، المظهر (داكن/فاتح)، حجم الخط (10–24) |
| **مشغّلات الفيديو** | مسار VLC وMPV (كشف تلقائي أو يدوي) |
| **إعدادات البحث** | عمق مسح المجلد (الحالي فقط / كل المجلدات الفرعية)، عدد المعالجات المتوازية (1–8)، حد النتائج (0 = بلا حد) |

**تلميحات:**
- **VLC** هو المفضل — يدعم القفز للتوقيت بدقة. **MPV** بديل ممتاز. إن لم يتوفر أي منهما يعمل مشغّل النظام لكن **بدون قفز للتوقيت**
- أي تغيير يُحفظ فورًا ويُطبَّق مباشرة (بدون إعادة تشغيل)

---

## 6) اختصارات لوحة المفاتيح

| الاختصار | الوظيفة |
|---|---|
| `Ctrl+F` | التركيز على خانة البحث |
| `F5` | إعادة مسح المجلد (تحديث) |
| `Ctrl+E` | تصدير Excel |
| `Ctrl+L` أو `/` | فتح تصفية النتائج |
| `Ctrl++` / `Ctrl+-` / `Ctrl+0` | تكبير / تصغير / إعادة ضبط حجم الخط |
| `Enter` (في النتائج) | فتح الفيديو |
| `Delete` | حذف نتيجة من الجدول |

---

## 7) نصائح عملية

- **اسم الملف يهم:** ليرتبط الترجمة بالفيديو تلقائيًا، اجعل اسم SRT قريبًا من اسم الفيديو (مثل `الحلقة1.srt` مع `الحلقة1.mp4`). اللاحقة التي يضيفها Buzz لا تضر
- **الفيديو في مجلد فرعي:** ClipSeek يبحث في المجلد ومجلد فرعي واحد أيضًا
- **الملفات الجديدة:** إذا أضفت ملفات SRT من Buzz بعد فتح التطبيق، ستظهر رسالة "تم العثور على X ملف ترجمة جديد" — ابدأ البحث لتحديث النتائج
- **حلقات كثيرة؟** أضف المجلدات إلى "المكتبة" وابحث فيها كلها مرة واحدة
- **دقة عربية:** البحث يتجاهل التشكيل والهمزات (أ/إ/آ→ا) و(ة→ه) و(ى→ي) تلقائيًا في الوضع الافتراضي

---

## 8) استكشاف الأخطاء وإصلاحها

| المشكلة | الحل |
|---|---|
| زر "فتح" يفتح مكان الملف بدل الفيديو | تأكد أن ملف SRT وملف الفيديو في نفس المجلد (أو مجلد فرعي مباشر) واسماهما متقاربان. إن لم يُربط تلقائيًا سيطلب اختيار الفيديو يدويًا |
| الفيديو يفتح من البداية لا عند الكلمة | ثبّت **VLC** (أو MPV) — مشغّل النظام لا يدعم القفز للتوقيت |
| لا تظهر نتائج رغم وجود ملفات | اضغط `F5` لتحديث المجلد، أو تأكد أن الملفات بصيغة SRT/VTT/TXT |
| التطبيق لا يفتح | تحقق من وجود ملف `clipseek_crash.txt` في مجلد المستخدم (`C:\Users\ك\clipseek_crash.txt`) وأرسله إن احتجت مساعدة |
| النتائج لا تظهر بعد إضافة ملفات Buzz | ابدأ بحثًا جديدًا — الفهرسة تلتقط الملفات الجديدة تلقائيًا |

---

## 9) أين تُحفظ بياناتك؟

| البيانات | المسار |
|---|---|
| الإعدادات | `C:\Users\<اسمك>\.clipseek\settings.json` |
| فهرس البحث (كاش) | `C:\Users\<اسمك>\.clipseek\index.db` |
| السجل | `C:\Users\<اسمك>\.clipseek\clipseek.log` |
| ملف الانهيار | `C:\Users\<اسمك>\clipseek_crash.txt` |

---

## 10) نموذج استخدام تعليمي

**السيناريو:** طالب يريد مراجعة درس "قواعد البيانات" من سلسلة من 20 حلقة.

1. شغّل ClipSeek → اختر مجلد السلسلة
2. اكتب في البحث: `"قواعد البيانات"` (بين علامتي اقتباس)
3. تظهر النتائج فورًا — فقط الحلقات التي ذكرت العبارة كاملة
4. اضغط **"📊 الملخص"** → تعرف أي حلقة تكررت فيها العبارة أكثر (الأهم)
5. اضغط **"▶ فتح"** على أفضل نتيجة → يقفز الفيديو للحظة التي قالها المدرس
6. عاود الكرة لأي كلمة/مصطلح آخر — الكل في ثوانٍ بدل تصفّح 20 حلقة

---

*وثّق هذا الدليل ليسهل على أي مستخدم جديد — طالبًا كان أو معلمًا — استرجاع أي لفظ أو كلمة من حلقاته في لحظات.*
